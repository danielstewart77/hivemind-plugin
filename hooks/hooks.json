{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,os,sys; cfg=os.environ.get('CLAUDE_CONFIG_DIR',os.path.expanduser('~/.claude')); inst=os.path.join(cfg,'plugins','installed_plugins.json'); data=json.load(open(inst)) if os.path.exists(inst) else {}; [exec(open(os.path.join(e[0]['installPath'],'hooks','sync_skills.py')).read()) or sys.exit(0) for k,e in data.get('plugins',{}).items() if e and 'hivemind' in k.lower() and os.path.exists(os.path.join(e[0]['installPath'],'hooks','sync_skills.py'))]\"",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "bash ~/.claude-config/hooks/session_start_identity.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude-config/hooks/soul_nudge.sh"
          }
        ]
      }
    ]
  }
}
